'use strict';

/**
 * Import(s)
 */

var expect = require('expect.js');
var ServerRouter = require(process.env.FENDJS_ROUTER_SERVER_COV ? '../lib-cov/' : '../');

/**
 * Test(s)
 */

describe('fendjs-router-server', function () {
});
